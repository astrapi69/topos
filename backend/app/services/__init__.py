"""Topos service layer.

Service modules wrap the SQLAlchemy session with one function per
business operation (no classes, no FastAPI types). Phase 4 adds
``containers.py``, ``items.py``, ``categories.py``, ``actions.py``.
"""
