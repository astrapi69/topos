# Topos i18n catalog (EN).
#
# Phase 3 bootstrap: minimal placeholder catalog. Phase 6 of the
# bootstrap repopulates this file with the full Topos UI string set
# once the React pages are scaffolded.
#
# The ``ui`` namespace is the i18n parity reference; the ``topos``
# namespace mirrors the few keys callers of the bootstrap reference.

ui:
  app:
    name: Topos
    description: Personal inventory tracker
topos:
  app:
    name: Topos
    description: Personal inventory tracker
